# 传感器组考核题

## 题目一：基于 OpenCV 的灯带识别与目标位移解算

### 1. 题目背景

机器人搭载一台固定安装的 RGB 相机，需要识别场景中的目标灯带，并根据灯带在图像中的位置变化计算目标相对于相机的位移。

目标由细长矩形灯带组成，两条灯带的实际长度、宽度以及在机器人上的安装位置已知。现假定相机始终 Pitch $= \theta$，以 Intel RealSense 为例，相机内参可以直接通过 `pyrealsense2` API 获取，其他的相机也类比。

> **Tips：** 可以找机械组做一个打印件，使相机保持 Pitch $= \theta$。

要求使用 OpenCV 完成灯带识别、矩形拟合、角点提取、PnP 位姿解算以及目标位移计算，依赖项建议通过 miniconda 创建虚拟环境安装。

### 2. 已知条件

1. 输入为 RGB 相机实时视频流。
2. 相机内参矩阵和畸变参数已知。
3. 每条灯带均可近似看作一个平面矩形，其实际尺寸已知：
   - $L$ 表示灯带实际长度；
   - $W$ 表示灯带实际宽度。
4. 灯带的安装倾角已知。

### 3. 任务要求

#### （1）灯带识别

使用 OpenCV 对输入图像进行处理，识别并提取细长灯带。

可采用图像预处理、阈值分割、轮廓提取等方法，并根据灯带的几何特征对候选区域进行筛选。

最终需要准确识别目标灯带。

#### （2）灯带矩形拟合与角点提取

对识别出的每一条灯带进行矩形拟合，对应一个旋转矩形框。

获得灯带的旋转矩形，并提取矩形的四个角点。

得到：

共得到 4 个二维图像角点。

要求保证各角点的编号顺序固定，使图像中的二维角点能够与灯带实际几何模型中的三维角点正确对应。

#### （3）建立目标三维模型

根据灯带的实际长度、宽度以及两条灯带之间的实际相对位置，在目标坐标系下建立两条矩形灯带对应的三维角点。

例如，每条灯带分别建立四个三维角点：

$$
P_{L1}^{w}, P_{L2}^{w}, P_{L3}^{w}, P_{L4}^{w}
$$

共得到 4 个三维特征点。

建立三维角点与图像二维角点之间的一一对应关系。

#### （4）目标位姿解算

利用：

- 4 个二维灯带角点；
- 对应的 4 个目标三维角点；
- 相机内参；
- 相机畸变参数；

使用 OpenCV 的：

```cpp
cv::solvePnP()
```

求解目标相对于相机坐标系的位姿。

得到旋转向量：

$$
rvec
$$

和平移向量：

$$
tvec
$$

其中 $X$、$Y$、$Z$ 表示目标在相机坐标系下的位置。

#### （5）目标位移计算

连续获取目标在不同时刻的位置，并且通过位置差异获取位移。

### 4. 输出要求

程序需要实时显示：

1. 原始相机图像；
2. 检测得到的灯带；
3. 旋转矩形框；
4. 灯带对应的 4 个角点；
5. 当前目标位置：

   ```text
   X = xxxx m  Y = xxxx m  Z = xxxx m
   ```

6. 目标相对于初始位置的位移：

   ```text
   dX = xxxx m  dY = xxxx m  dZ = xxxx m  Distance = xxxx m
   ```

最终提交完整程序及运行结果。

> **Tips：** 提交 GitHub 仓库时要写好 `README.md`，附带一段代码运行效果展示视频（`mp4` 格式），代码实现和文件组织要有逻辑性！

---

## 题目二：基于 EKF 的 IMU 姿态解算

### 1. 题目背景

机器人搭载一个六轴 IMU，可实时输出：

- 三轴角速度：$\omega_x, \omega_y, \omega_z$
- 三轴加速度：$a_x, a_y, a_z$

其中陀螺仪具有零偏和积分漂移，加速度计测量值中则同时包含重力和机器人运动产生的线加速度。

要求设计一个基于扩展卡尔曼滤波（EKF）的姿态估计算法，实现机器人姿态的实时估计。

### 2. 已知条件

IMU 数据以固定频率输出：

$$
200\ \text{Hz}
$$

每帧数据包括：

```text
timestamp gyro_x gyro_y gyro_z acc_x acc_y acc_z
```

角速度单位统一为：$\mathrm{rad/s}$；加速度单位统一为：$g$。

初始时刻机器人静止，并且可以利用前若干帧 IMU 数据估计陀螺仪零偏。

除 IMU 原始数据外，另提供一套由激光-惯性里程计（FAST-LIO）输出的位姿结果作为观测数据：`pose_cov.csv`。该文件与 IMU 同频（约 200 Hz），每帧包含：

- `timestamp`
- `x, y, z` 位置（m）
- `qx, qy, qz, qw` 姿态四元数（单位四元数，w 在最后）
- `cov_00 ... cov_55` 与位姿对应的 $6 \times 6$ 协方差（行主序）

其中协方差的索引约定为：0–2 对应位置 $x, y, z$，3–5 对应姿态 $(roll, pitch, yaw)$，对角元素即对应的方差。

两份数据来自同一段录制，`timestamp` 为同一时间基准，可按时间对齐使用。

> **提示：** IMU 的加速度单位为 $g$（静止时 $\left|a\right| = 1$），非 $\mathrm{m/s^2}$；如需要 $\mathrm{m/s^2}$ 请自行乘以 $9.81$。

### 3. 状态设计

**必做：** 估计姿态，状态量至少包含：

- 姿态四元数 $q$（4 维）
- 陀螺仪零偏 $\boldsymbol{b}_g$（3 维）

**选做（位置估计）：** 在此基础上进一步估计位置，可扩展状态量为：

- 位置 $\boldsymbol{p}$（3 维）、速度 $\boldsymbol{v}$（3 维）
- 加速度计零偏 $\boldsymbol{b}_a$（3 维）

误差状态 $\delta \boldsymbol{x}$：姿态误差用旋转向量表示（3 维，四元数标称 4 维但误差只占 3 维）。

必做：

$$
\delta \boldsymbol{x} = [\delta \boldsymbol{\theta}, \delta \boldsymbol{b}_g]
$$

共 6 维；

做位置选做：

$$
\delta \boldsymbol{x} = [\delta \boldsymbol{p}, \delta \boldsymbol{v}, \delta \boldsymbol{\theta}, \delta \boldsymbol{b}_g, \delta \boldsymbol{b}_a]
$$

共 15 维。

### 4. 任务要求

#### （1）IMU 数据预处理

完成：

- 数据读取；
- 时间戳处理；
- 相邻两帧时间间隔 $\Delta t$ 计算；
- 初始静止阶段陀螺仪零偏 $\boldsymbol{b}_g$ 的估计；
- （选做）加速度计零偏 $\boldsymbol{b}_a$ 的估计（静止时加计只感受重力）；
- 必要的数据异常值处理。

#### （2）EKF 预测过程

用 IMU 在相邻两帧间积分预测状态（与 FAST-LIO 的传播思路一致）。

**姿态预测（必做）：**

零偏补偿：

$$
\boldsymbol{\omega} = \boldsymbol{\omega}_m - \boldsymbol{b}_g
$$

姿态由角速度积分：

$$
\dot{q} = \frac{1}{2} q \otimes \omega,\quad \dot{\boldsymbol{b}}_g = \boldsymbol{0}
$$

**位置预测（选做）：** 位置由速度积分、速度由加计（去零偏、去重力、旋转到世界系）积分（自行推导）。

加速度计量测记为 $\boldsymbol{a}_m$，去零偏后为：

$$
\tilde{\boldsymbol{a}} = \boldsymbol{a}_m - \boldsymbol{b}_a
$$

$\boldsymbol{g}$ 为重力向量（注意 IMU 数据中加速度单位为 $g$，需换算为 $\mathrm{m/s^2}$ 或相应取 $\left|g\right| = 1$）。

要求完成该模型的 EKF 预测（状态递推、$\boldsymbol{P}$ 的预测、$\boldsymbol{Q}$ 的设计；$\Delta t$ 为相邻两帧间隔，$\boldsymbol{Q}$ 需包含陀螺零偏的随机游走，做位置选做时再加加计零偏项）。

> **提示：** 四元数更新后需归一化。

#### （3）EKF 观测更新

利用 `pose_cov.csv` 中激光-惯性里程计输出的位姿作为观测。

- **（必做）姿态观测：** 观测值为 `pose_cov.csv` 的 `qx, qy, qz, qw`；
- **（选做）位置观测：** 观测值为 `pose_cov.csv` 的 `x, y, z`；
- 观测噪声取自 `pose_cov.csv` 的协方差对角项（姿态取 `cov_33, cov_44, cov_55`；位置取 `cov_00, cov_11, cov_22`）；
- 观测时刻与 IMU 数据按 `timestamp` 对齐。

要求完成该观测下的 EKF 更新（残差、增益、状态与协方差更新）。姿态观测使 Roll、Pitch、Yaw 全部可观（尤其 Yaw 是加速度计重力观测无法约束的）。

#### （4）位姿输出

- 姿态：将最终四元数转换为欧拉角（Roll, Pitch, Yaw）并输出或保存。（必做）
- 位置：输出 `x, y, z`。（选做）

要求至少绘制：

```text
Roll - Time
Pitch - Time
Yaw - Time
```

三条姿态变化曲线；做位置选做时，再补：

```text
x - Time
y - Time
z - Time
```

### 5. 输出要求

最终提交：

1. EKF 完整代码；
2. IMU 数据读取程序；
3. Roll、Pitch、Yaw 数据及趋势曲线（选做位置估计时，附 `x`、`y`、`z` 数据及曲线）；
4. 一组实验结果；
5. 简单说明 EKF 的：状态量；预测模型；观测模型；$\boldsymbol{Q}$ 与 $\boldsymbol{R}$ 的作用。